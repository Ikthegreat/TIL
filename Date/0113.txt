복습

- add \- commit \- push (기본적인 흐름)

Clone

- git clone 'url'

- git Folder 가져오기 (github to local) (연동)

Pull

- git pull origin master

- 반대로 서버에서 Local로 Commit 가져오기

touch .gitignore

- Ignore Option 추가 (git으로 관리하지 않는 File)

/는 디렉토리

* 은 모든 이라는 뜻을 내포하기도 함

git log --oneline --graph

git reset (--hard) (log난수)

- 해당 시점으로 reset (Skeleton Code) 시점 이동 가능
