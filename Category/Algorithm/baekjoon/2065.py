M, t, N = list(map(int, input().split()))

moving_time = 0
left_list = []
right_list = []
for n in range(N):
    arrive_time, arrived_way = list(map(int, input().split()))
    if arrived_way == left:
        left_list.append(1)
    else:
        right_list.append(1)
    if len(left_list) == M:
        if 
    continue

